CREATE TABLE contact(
                        id LONG NOT NULL PRIMARY KEY AUTO_INCREMENT,
                        name VARCHAR(255),
                        phoneNumber VARCHAR(255),
                        address VARCHAR(255),
                        email VARCHAR(255),
                        role VARCHAR(255)
);
